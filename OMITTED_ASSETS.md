# Omitted from this git tree

Keep these on the **game server** (or add later via Git LFS):

| Item | Why |
|------|-----|
| `fivex_flexa/gallery/*` | Runtime camera photos |
| `fivex_flexa/html/_refs/*` | Design reference screenshots |
| `fivex_staffvest/stream/*.ydd` (and other clothing assets) | Large binary stream files |
| `fivex_staffvest/EDIT ME.psd` + large PNG renders | Source art (~100MB+) |
| `fivex_zombies/html/audio/alarm.ogg` | Binary audio — copy from live server into `html/audio/` |

After clone, copy those assets from your existing `[fivex]` install if a resource needs them.
