# FiveX Scripts

Public monorepo for FiveX FiveM resources (112 Play Theory / FiveX).

Layout: one folder per resource on `main`. Version checks use `fivex_versioncheck` with per-script GitHub Releases (tags like `fivex_flexa-v1.2.1`).

Seed commit — full resources land next.
